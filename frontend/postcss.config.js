export default {
  plugins: {
    // Đảm bảo tailwindcss là plugin đầu tiên và duy nhất cần thiết
    tailwindcss: {},
    // Tùy chọn: Thường không cần autoprefixer trong v4 vì nó đã được tích hợp
    // autoprefixer: {}, 
  },
}